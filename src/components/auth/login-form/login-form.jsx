import React from "react";

const LoginFormComponent = ({ onSubmit }) => {

    return <div>
        Press <button onClick={onSubmit}>here</button> to log in!
    </div>
}

export default LoginFormComponent